/*
const ReactGA = require('react-ga')

ReactGA.initialize(`GA CODE GOES HERE`)
ReactGA.set({
  appName: 'gatsby-template'
})

exports.onRouteUpdate = (state) => {
  ReactGA.pageview(state.location.pathname)
}
 */